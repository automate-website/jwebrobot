name: Action with complex criteria
steps:
  - open: 'open.url1'
  - open:
      url: 'open.url'
      if: 'open.if'
      unless: 'open.unless'
  - ensure: 'ensure.selector1'
  - ensure:
      selector: 'ensure.selector'
      text: 'ensure.text'
      timeout: 'ensure.timeout'
      value: 'ensure.value'
      if: 'ensure.if'
      unless: 'ensure.unless'
      #parent: 'ensure.parent'
  - move: 'move.selector1'
  - move:
      selector: 'move.selector'
      text: 'move.text'
      timeout: 'ensure.timeout'
      if: 'move.if'
      unless: 'move.unless'
      # parent: 'move.parent'
  - click: 'click.selector1'
  - click:
      selector: 'click.selector'
      text: 'click.text'
      timeout: 'click.timeout'
      if: 'click.if'
      unless: 'click.unless'
      # parent: 'click.parent'
  - select: 'select.selector1'
  - select:
      selector: 'select.selector'
      text: 'select.text'
      timeout: 'select.timeout'
      value: 'select.value'
      if: 'select.if'
      unless: 'select.unless'
      # parent: 'select.parent'
  - enter: 'enter.input1'
  - enter:
      input: 'enter.input'
      clear: true
      text: 'enter.text'
      selector: 'enter.selector'
      timeout: 'enter.timeout'
      if: 'enter.if'
      unless: 'enter.unless'
      # parent: 'enter.parent'
  - wait: 'wait.time1'
  - wait:
      time: 'wait.time'
      if: 'wait.if'
      unless: 'wait.unless'
  - include: 'include.scenario1'
  - include:
      scenario: 'include.scenario'
      if: 'include.if'
      unless: 'include.unless'
  # - store:
